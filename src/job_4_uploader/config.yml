# Uploader configuration (simplified for volume creation)

credentials:
  username: ""
  password: ""

series:
  novel_id: ""        # Required
  # base_url: "https://docln.sbs"   # Optional override (defaults to docln.sbs)

paths:
  # Folder containing volume directories; each subfolder = a volume; files inside are chapters
  input: "4_output_md"   # Relative to project root or absolute path
  # Optional custom log directory (relative or absolute). If empty, defaults to inventory/logs
  log: "inventory/logs/uploader"
