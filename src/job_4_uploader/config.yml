# Uploader configuration

credentials:
  username: "Phamduytuan006@gmail.com"
  password: "cocaiccc"

series:
  novel_id: "23258"        # Required
  # base_url: "https://docln.sbs"   # Optional override
  # img_base_url: ""               # Optional image base path

paths:
  input: "4_output_md/kyoukai_senjou_no_horizon"            # Folder containing volumes
  log: "inventory/logs/uploader"  # Log folder

retry:
  delay_on_429: 10    # seconds to wait after 429
  max_retries: 20      # max retries on 429
