# EPUB to Markdown converter config
# Required: directory containing .epub files
input_dir: 0_epub

# Required: output directory for extracted markdown
output_dir: 1_input_md

# Optional flags
overwrite: true        # overwrite existing chapter files
combine: false         # also write a single combined markdown file per book
extract_images: true   # extract images to <book_dir>/_assets/
