paths:
  input: "3_output_segment"                   # Root directory containing YAML segment files
  output: "4_output_md"                       # Destination root for merged Markdown

processing:
  write_title_header: true            # Write '## <title>' at top of the output file
